# Copy to .env and fill in. For personal use only — do not commit .env

# Airflow (generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
AIRFLOW__CORE__FERNET_KEY=MfE9NOHPb2-YxMCGgehMnc9jOOLGBVkQ7CYmtww6q-A=

# FRED (https://fred.stlouisfed.org/docs/api/api_key.html)
FRED_API_KEY=api

# OKX: public market data only — no API keys required for candles

# MinIO (match docker-compose defaults or override)
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=minio12345
S3_BUCKET=quant-lake
